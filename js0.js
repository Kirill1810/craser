const paragraphs = document.querySelectorAll('div');


paragraphs.forEach(function(div) {
    div.classList.add('d1');
  });


  const paragaphs = document.querySelectorAll('div');


paragaphs.forEach(function(div) {
    div.classList.add('d2');
  });

  const paragahs = document.querySelectorAll('div');

  paragaphs.forEach(function(div) {
    div.classList.add('d3');

  
  })
  
  





 